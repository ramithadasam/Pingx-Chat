# PingX frontend — environment variables
# Copy this file: cp .env.example .env

# Local dev: point at the local API server (port must match api-server PORT).
# Leave empty or unset in production (the reverse proxy handles /api/* routing).
VITE_API_BASE_URL=http://localhost:8080
